#!/bin/sh

VERSION=4.5.0
PYIOC=/dls_sw/prod/python3/RHEL7-x86_64/softioc/$VERSION
PYIOC=$PYIOC/prefix/bin/pythonSoftIOC

[ -n "$1" ]  &&  export EPICS_CAS_SERVER_PORT=6064

cd "$(dirname "$0")"
exec $PYIOC -u concentrator.py "$@"
